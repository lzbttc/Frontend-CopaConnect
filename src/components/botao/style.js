import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    alignItems: 'center',
  },

  botao: {
    width: 350,
    height: 60,

    justifyContent: 'center',
    alignItems: 'center',

    position: 'relative',
  },

  svg: {
    position: 'absolute',
    width: '100%',
    height: '100%',
  },

  texto: {
    fontFamily: 'Changa',
    fontSize: 20,

    color: '#0A1E4D',

    textAlign: 'center',
  },
});